# ===========================================================
# SALLY Frontend - Environment Configuration
# ===========================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# ===========================================================

# --------------- Backend API (Required) ---------------
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# --------------- Feature Flags ---------------
NEXT_PUBLIC_ENABLE_PREDICTIONS=true

# --------------- Docs Site URL ---------------
NEXT_PUBLIC_DOCS_URL=http://localhost:3001

# --------------- Environment ---------------
NODE_ENV=development

# --------------- Firebase (Optional for local dev) ---------------
# Without these, the frontend login page will show an error.
# For local development, you can use the backend's mock auth
# endpoint directly (POST /auth/login) via Swagger or curl.
#
# To enable the full login UI, create a Firebase project at
# https://console.firebase.google.com and copy the web app config:
# NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
# NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
