# Ensure React is properly deduplicated across workspaces
# This prevents multiple React instances during SSR
legacy-peer-deps=false
strict-peer-deps=false

# Hoist common dependencies to root for better caching
hoist=true

# Ensure optional dependencies (SWC native binaries) are installed
optional=true
